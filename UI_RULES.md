# Claude UI 改造与审查规范 (v1.0)

本规范为后续本项目的唯一视觉指导方针。必须在后续的组件封装和工具页面改造中严格遵照执行。

## 1. 颜色系统 (Color System)
**禁止使用任何纯黑(`bg-black`, `#000`, `rgba(0,0,0)`)**，所有深色背景必须替换为白昼渐变或暖灰系统。

- **页面主底色**：`bg-[#FAF9F6]` (暖白/琥珀灰) 或纯白 `bg-white`。
- **品牌高亮色 (CTA / 警示)**：`#D97706` (琥珀橙, Amber-600)。所有醒目按钮、强调数字、关键链接必须用这个颜色。
- **次要高亮色**：棕色系（如 `#6B6660`、`#444`）用于辅助展示。
- **边框与分割线**：`border-[#E5E1D8]`，Hover态使用 `hover:border-[#D97706]`。
- **卡片/微小背景块**：用 `#F3F1ED` 或纯白，保持呼吸感。

## 2. 空间与退距 (Spacing & Padding)
**严禁“内容贴边”的窒息感**。之前所有 `px-2`/`px-4` 等窄边距全面摒弃。

- **全局容器**：至少 `max-w-5xl` 并附带 `mx-auto` 和 `px-8` (移动端) 或 `px-16` (PC端) 以上退距。
- **内容块间距**：不同模块之间用 `mb-16` 到 `mb-24` 进行分割，保证留白。
- **组件内部间距**：Card 必须至少有 `p-8` 或 `p-10`，让内部文字有充足空间。

## 3. 字体与排版 (Typography)
- **字体族**：全站默认英文使用 **Plus Jakarta Sans**，中文强制降级至 **Noto Sans SC**，不再使用 Inter。
- **字重分级**：标题必须用 `font-black` (重磅字重)，正文使用 `font-medium` 或常规。
- **字号拉大**：
  - 超大英雄标题使用 `text-6xl md:text-8xl`
  - 卡片大标题使用 `text-4xl md:text-5xl`
  - 二级辅助标注使用 `text-sm font-mono uppercase tracking-wide`
- **行高**：正文行高必须放大至 `leading-relaxed` 或 `leading-loose`。

## 4. 动效与交互 (Motion)
- 遵循 Framer Motion 的克制原则，仅保留 `opacity`, `y` (位移), 和 `scale`。
- 绝不要花哨的翻转、抖动弹跳。
- **Hover 态**：使用微小的 `scale-105` 或加深 `box-shadow`。
- 页面加载一律采用 `spring` 缓动，`stiffness: 100`, `damping: 20` 这一组合。

## 5. 组件审查 (Checklist)
进行任何工具页或新页面写入时，必须核对：
- [ ] 有没有漏掉纯黑色的边框或背景？（必须改为 `#FAF9F6` 等）
- [ ] `div` 的内边距有没有给够（至少 p-6，最好 p-10）？
- [ ] 文字是否对比度达标（深灰字配白底）且不要过度挤压？
- [ ] 是否在强调用色时使用了规定的琥珀橙 `#D97706`？
